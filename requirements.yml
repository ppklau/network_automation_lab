---
# ACME Investments Network Automation Lab — Ansible collection dependencies
#
# Install with:
#   ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansible.netcommon
  - name: arista.eos
  - name: frr.frr
