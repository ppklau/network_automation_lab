! ─────────────────────────────────────────────────────────────────────────────
! Bootstrap configuration — leaf-lon-01
! ACME Investments Network Automation Lab
!
! This is a BOOTSTRAP config only. Management0 IP is set by containerlab.
! Full device configuration is pushed by Ansible from the SoT:
!   ansible-playbook playbooks/push_configs.yml --limit leaf-lon-01
! ─────────────────────────────────────────────────────────────────────────────
!
hostname leaf-lon-01
!
no aaa root
!
username admin privilege 15 role network-admin secret admin
!
management api http-commands
   no protocol https
   no shutdown
!
management ssh
   no shutdown
!
aaa authentication login default local
aaa authorization exec default local
!
logging on
!
end
